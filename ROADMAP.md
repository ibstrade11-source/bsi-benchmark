# نقشهٔ راه bsi-benchmark

این سند خلاصهٔ یک بازبینی راهبردی (توسط ChatGPT، تاریخ ۲۰۲۶-۰۷-مرداد) است
که پیشنهاد کرد `bsi-benchmark` از یک ابزار اختصاصی BSI به یک **پلتفرم عمومی
ارزیابی چارچوب‌های تحلیل مبتنی بر LLM** تبدیل شود، به‌همراه تصمیم‌گیری
دربارهٔ کدام بخش‌ها الان قابل‌ساخت‌اند و کدام نیازمند منابعی هستند که فعلاً
نداریم.

## اصل راهبردی پذیرفته‌شده

بزرگ‌ترین ریسک اعتبار فعلی: «سازنده، هم روش (BSI) را ساخته، هم آزمون
(benchmark) را.» راه‌حل بلندمدت جداسازی مفهومی benchmark از BSI است —
BSI باید یکی از چند framework ثبت‌شده باشد، نه چیزی که در هستهٔ ابزار
hardcode شده.

## پیاده‌سازی‌شده (۲۰۲۶-۰۷-مرداد)

سه اولویت اول از پیشنهاد ۱۲موردی، بر اساس معیار «کم‌هزینه و بدون نیاز به
منبع بیرونی» انتخاب و پیاده‌سازی شدند:

### ۱. `stats.py` — تحلیل آماری استاندارد
تجمیع چند رکورد `self-compare` با:
- فاصلهٔ اطمینان ۹۵٪ (bootstrap، نه فرض نرمال‌بودن)
- Cohen's d_z (اندازهٔ اثر جفت‌شده)
- آزمون Wilcoxon signed-rank (تقریب نرمال، با هشدار صریح برای n<10)
- شمارش win/tie/loss

**تصمیم فنی:** بدون وابستگی به scipy/numpy — چون این ابزار روی
Termux/Android اجرا می‌شود و کامپایل scipy در آن محیط معمولاً دردسرساز
است. صحت اعداد (W، Cohen's d) در برابر scipy/numpy واقعی چک و تأیید شد
(نتایج دقیقاً یکسان؛ فقط p-value به‌خاطر تقریب نرمال در برابر تست دقیق
اندکی فرق دارد که در کد صراحتاً اعلام می‌شود).

**محدودیت صادقانه (در خروجی خودِ ابزار هم چاپ می‌شود):** این آمار همهٔ
معیارها را از همهٔ رکوردها در یک نمونه ادغام می‌کند — معیارهای داخل یک
رکورد (همان مقاله، همان تحلیلگر) مستقل از هم نیستند. N ظاهری بزرگ‌تر از
تعداد مشاهدات مستقل واقعی است.

دستور: `bsi-benchmark stats --input results/self_compare/*.json --output out/agg`

### ۲. متادیتای تکمیلی مدل (`run_metadata.py`)
فیلدهای جدید (همه اختیاری، پیش‌فرض None، سازگار با رکوردهای قدیمی):
`provider_name`, `model_version`, `temperature`, `max_tokens`,
`prompt_version`, `framework_name`.

دستور: `bsi-benchmark compare ... --temperature 0.7 --max-tokens 4000 --prompt-version v3.4.2`

### ۳. معماری افزونهٔ حداقلی (`frameworks/registry.py`)
یک `FrameworkRegistry` ساده. BSI همان کدی که همیشه داشت (`prompt_loader.py`)
را دارد، فقط حالا به‌عنوان یک ورودی در رجیستری (`registry.register(...)`)
ثبت شده، نه چیزی که در سراسر کد hardcode باشد.

**صادقانه:** این هنوز `compare`/`self-compare` را framework-agnostic نمی‌کند
— آن یک بازنویسی بزرگ‌تر و پرریسک‌تر است (نگاه کنید به بخش «بلندمدت»
پایین). این فقط seam را ایجاد می‌کند تا framework دوم بدون دست‌زدن به کد
BSI اضافه شود.

دستور: `bsi-benchmark frameworks`

## نکتهٔ حیاتی: تمایز Blind Evaluation از Self-Compare

پیشنهاد ChatGPT شامل «Blind Evaluation» (داور سوم‌شخص، بدون اطلاع از اینکه
کدام خروجی از کدام framework است) بود. این **آگاهانه از `self-compare`
جدا نگه داشته شد** چون این دو ابزار متفاوت با اهداف متفاوت‌اند:

- **`self-compare`** (موجود): تحلیلگر خودش هر دو تحلیل (raw/bsi) را
  نوشته، خودش می‌داند کدام کدام است، معیار خودش را انتخاب می‌کند. هدف:
  خودارزیابی صادقانه با معیار غیرتحمیلی.
- **Blind evaluation** (پیاده‌نشده، عمداً): داور مستقل که نمی‌داند کدام
  خروجی از کدام framework است. هدف: کاهش سوگیری داور.

این دو را با هم قاطی نکنید — تلاش برای «کورکردن» self-compare (پنهان‌کردن
برچسب raw/bsi از خودِ تحلیلگر) مستقیماً ارزش خودارزیابی مستقل را نابود
می‌کند (این را در گفتگوی پروژه صریحاً رد کردیم).

## بلندمدت — نیازمند منابعی که فعلاً نداریم

| مورد | چرا الان نه |
|---|---|
| Ground Truth مستقل | نیازمند پنل متخصص انسانی |
| Blind Evaluation واقعی (داور سوم‌شخص) | نیازمند فراخوان API واقعی برای هر داور؛ باید مسیر کاملاً جدا از self-compare باشد |
| چند داور هم‌زمان (Human+Claude+GPT+Gemini) | هزینهٔ API چندبرابر؛ نیازمند کلید چند provider |
| Leaderboard عمومی | این دیگر یک محصول وب مجزاست، نه یک CLI feature |
| Dataset استاندارد چندحوزه‌ای (اخبار/حقوقی/پزشکی/...) | نیازمند gather و curation قابل‌توجه |
| Error Taxonomy (Hallucination/Bias/...) | نیازمند طبقه‌بندی دستی یا یک classifier جداگانه که خودش نیاز به اعتبارسنجی دارد |
| Reproducibility Package کامل (config+dataset+versions+seed بسته‌بندی‌شده) | ارزشمند اما کار توسعهٔ متوسط؛ فعلاً `run_metadata` بخشی از آن را پوشش می‌دهد |
| Plugin Architecture کامل (`compare`/`self-compare` واقعاً framework-agnostic) | ریسک شکستن رفتار فعلی BSI بالاست؛ باید با تست رگرسیون سنگین انجام شود |
| «سه سطح ارزیابی» (Output/Reasoning/Epistemic Reliability) | نیازمند بررسی که آیا این واقعاً چیزی فراتر از D1-D7 موجود BSI اضافه می‌کند یا فقط بازنام‌گذاری است |

## اصل انتخاب اولویت (برای جلسات آینده)

هر آیتم جدید از این لیست قبل از پیاده‌سازی باید این تست را پاس کند:
«آیا بدون منبع بیرونی (API پولی، متخصص انسانی، زیرساخت وب) قابل‌ساخت و
قابل‌تست است؟» اگر جواب نه بود، در همین بخش «بلندمدت» می‌ماند تا وقتی آن
منبع فراهم شود.
