BSI Benchmark

روش‌شناسی ارزیابی چارچوب‌های تحلیلی مدل‌های زبانی

نوع سند: مشخصات روش‌شناسی
وضعیت: پیش‌نویس نسخه ۰.۱
دامنه: مستقل از چارچوب
سند مادر: "BENCHMARK_SPEC.md"
سند مکمل: "ONTOLOGY.md"

---

1. دامنه و هدف

این سند روش اجرای BSI Benchmark را مشخص می‌کند.

در این روش‌شناسی، مفاهیم و روابط تعریف‌شده در "ONTOLOGY.md" به یک پروتکل تجربی قابل اجرا تبدیل می‌شوند. بنابراین، این سند محل تعریف مجدد مفاهیم هستی‌شناختی نیست؛ بلکه مشخص می‌کند این مفاهیم چگونه در طراحی آزمایش، تولید خروجی، ارزیابی، مقایسه، ثبت داده و تحلیل نتایج به کار گرفته می‌شوند.

هدف اصلی روش‌شناسی، فراهم‌کردن یک رویه کنترل‌شده برای مطالعه این پرسش است:

«استفاده از یک چارچوب تحلیلی ساختارمند، در تعامل با یک مدل زبانی و یک مصنوع مشخص، چه تغییری در کیفیت و ارزش تحلیلی خروجی ایجاد می‌کند و این تغییر چه رابطه‌ای با پایداری، انحراف، تحقق چارچوب و بار اجرایی آن دارد؟»

این پرسش باید در سطح چارچوب × مدل × مصنوع مطالعه شود و نباید از یک اجرای منفرد به یک ویژگی ذاتی چارچوب یا مدل تعمیم داده شود.

---

2. منطق کلی طراحی

هر مطالعه Benchmark باید، در حد امکان، یک مقایسه کنترل‌شده میان حداقل دو وضعیت ایجاد کند:

- وضعیت مرجع؛
- وضعیت مداخله.

در حالت پایه، وضعیت مرجع "RAW" و وضعیت مداخله "Framework" است.

هر دو وضعیت باید تا حد امکان یک مسئله تحلیلی واحد را روی یک مصنوع واحد اجرا کنند.

طرح پایه:

[
[Artifact + Model + Task \rightarrow {RAW,\ Framework}]
]

خروجی‌های حاصل سپس تحت یک فرآیند ارزیابی مشترک قرار می‌گیرند.

این ساختار امکان برآورد تغییر مشاهده‌شده را فراهم می‌کند، بدون آنکه از ابتدا نتیجه به چارچوب نسبت داده شود.

---

3. تعریف سؤال و فرضیه پیش از اجرا

پیش از آغاز اجرای Benchmark باید سؤال ارزیابی مشخص شود.

سؤال باید روشن کند که مطالعه قصد بررسی کدام‌یک از موارد زیر را دارد:

- تغییر کیفیت تحلیلی؛
- ارزش تحلیلی افزوده؛
- تغییر در ثبات خروجی؛
- تغییر در انحراف (Drift)؛
- تحقق یا عدم تحقق الزامات چارچوب؛
- هزینه یا بار اجرایی؛
- یا ترکیبی از این موارد.

در مطالعات مقایسه‌ای، فرضیه‌ها باید پیش از مشاهده نتایج اصلی ثبت شوند.

طراحی می‌تواند یک فرضیه جهت‌دار یا غیرجهت‌دار درباره تفاوت دو وضعیت داشته باشد، اما Benchmark نباید وجود بهبود را مفروض بگیرد.

---

4. انتخاب مصنوع

4.1. واحد انتخاب

مصنوع (Artifact) باید پیش از تولید خروجی انتخاب و شناسه‌گذاری شود.

هر مصنوع باید دارای شناسه یکتا و اطلاعات کافی درباره منشأ و سابقه آن (Provenance) برای بازیابی نسخه مورد استفاده باشد.

اطلاعات حداقلی عبارت‌اند از:

- شناسه مصنوع؛
- عنوان؛
- نوع؛
- حوزه؛
- منبع؛
- نسخه یا تاریخ بازیابی؛
- وضعیت متن کامل؛
- زبان؛
- هرگونه پیش‌پردازش اعمال‌شده.

4.2. ثبات مصنوع

در مقایسه مستقیم RAW و Framework، محتوای ورودی باید یکسان باشد.

اگر به دلیل محدودیت فنی، دو وضعیت متن متفاوتی دریافت کنند، این تفاوت باید به‌عنوان یک متغیر اجرایی ثبت شود.

چنین مقایسه‌ای نباید بدون ذکر این محدودیت به‌عنوان یک مقایسه کاملاً کنترل‌شده میان چارچوب‌ها گزارش شود.

---

5. انتخاب مدل

برای هر Run باید Model و Provider به‌صورت دقیق ثبت شوند.

حداقل اطلاعات عبارت‌اند از:

- ارائه‌دهنده؛
- شناسه مدل؛
- نسخه مدل، در صورت قابل دسترس بودن؛
- پارامترهای تولید؛
- محدودیت زمینه؛
- ابزارهای فعال؛
- تاریخ و زمان اجرا.

در صورتی که نسخه دقیق Model قابل تعیین نباشد، این عدم قطعیت باید در Record ثبت شود.

Model نباید صرفاً به‌عنوان ابزار تولید خروجی تلقی شود؛ در طراحی چندمدلی، تفاوت Modelها بخشی از ساختار تجربی است.

---

6. تعریف وظیفه

Task باید پیش از اجرا مشخص شود.

Task باید برای هر دو Condition یکسان باقی بماند، مگر اینکه طراحی مطالعه صراحتاً تغییر Task را موضوع آزمایش قرار داده باشد.

تعریف Task باید به اندازه‌ای دقیق باشد که:

1. هدف تحلیل روشن باشد؛
2. خروجی مورد انتظار قابل تشخیص باشد؛
3. اجرای مستقل در Conditions امکان‌پذیر باشد؛
4. ارزیابی بعدی بر اساس همان هدف انجام شود.

---

7. طراحی وضعیت RAW

RAW Condition باید یک خط مبنای عملیاتی معتبر ایجاد کند.

RAW به معنای حذف تمام دستورالعمل‌های عمومی نیست. Model باید بداند چه Taskی را باید انجام دهد.

بااین‌حال، RAW نباید حاوی مداخله تحلیلی‌ای باشد که عملاً همان Framework مورد ارزیابی را بازسازی کند.

Prompt پایه، محدودیت‌ها و منابع مورد استفاده در RAW باید ثبت شوند.

---

8. طراحی وضعیت Framework

Framework Condition باید همان Task را با استفاده از نسخه مشخص Framework اجرا کند.

برای هر Framework باید ثبت شود:

- شناسه Framework؛
- نسخه؛
- مشخصات مورد استفاده؛
- Prompt یا حامل اجرایی (Execution Carrier)؛
- مراحل اجرایی؛
- وابستگی به ابزار یا منابع خارجی؛
- الزامات اجباری؛
- الزامات اختیاری.

اگر Framework چندمرحله‌ای باشد، ترتیب مراحل باید قابل بازسازی باشد.

---

9. هم‌ترازی Conditions

اعتبار مقایسه به هم‌ترازی شرایط وابسته است.

عامل| RAW| Framework
Artifact| یکسان| یکسان
Task| یکسان| یکسان
Model| یکسان در مقایسه درون‌مدلی| یکسان
زبان| یکسان| یکسان
منابع خارجی| کنترل‌شده| کنترل‌شده
محدودیت زمانی| یکسان یا ثبت‌شده| یکسان یا ثبت‌شده
محدودیت زمینه| ثبت‌شده| ثبت‌شده
تنظیمات تولید| ثابت یا ثبت‌شده| ثابت یا ثبت‌شده

هر تفاوت اجتناب‌ناپذیر باید در داده اجرایی ثبت شود.

---

10. Prompt و حامل اجرایی

Prompt وسیله اجرای Task و انتقال Framework به Model است و نباید با خود Framework یکی گرفته شود.

بنابراین، تغییر Prompt می‌تواند یک تغییر روش‌شناختی محسوب شود، حتی اگر مشخصات Framework ثابت مانده باشد.

برای هر Run باید امکان شناسایی نسخه Prompt وجود داشته باشد.

در صورت استفاده از Prompt Template، نسخه Template نیز باید ثبت شود.

---

11. اجرای مستقل

هر Run مستقل باید در یک زمینه اجرایی مستقل انجام شود.

مگر آنکه طراحی آزمایش صراحتاً به اجرای وابسته نیاز داشته باشد، نباید:

- خروجی Run قبلی وارد Run جدید شود؛
- تاریخچه مکالمه قبلی باقی بماند؛
- وضعیت پنهان سیستم میان Conditions منتقل شود؛
- نتیجه یک Condition به Condition دیگر افشا شود.

هدف این اصل جلوگیری از متغیرهای مخدوش‌کننده ناشی از وضعیت باقی‌مانده است.

---

12. تصادفی‌بودن و تنظیمات اجرا

در صورتی که Model یا Provider امکان کنترل تصادفی‌بودن را داشته باشد، تنظیمات مربوطه باید ثبت شوند.

در صورت امکان:

- seed؛
- temperature؛
- top-p؛
- سایر پارامترهای مؤثر

باید ذخیره شوند.

اگر Provider کنترل کامل تصادفی‌بودن را ارائه نمی‌کند، این موضوع باید به‌عنوان محدودیت ثبت شود.

---

13. اجرای تکرارشونده

برای مطالعه نوسان خروجی و Drift، یک Condition باید در صورت امکان بیش از یک‌بار اجرا شود.

تکرارها باید:

- مستقل باشند؛
- با Artifact ثابت انجام شوند؛
- Task ثابت داشته باشند؛
- Model و configuration قابل مقایسه داشته باشند.

تعداد تکرارها باید در گزارش نهایی ذکر شود.

تکرار بیشتر به‌تنهایی جایگزین طراحی کنترل‌شده نیست.

---

14. طراحی مقایسه‌ای چندمدلی

در مطالعات چندمدلی، هر Model باید تا حد امکان همان مجموعه Conditions را دریافت کند.

طرح پایه:

[
[{M_1,M_2,\ldots,M_n}\times{RAW,Framework}\times{A_1,A_2,\ldots,A_k}]
]

این طراحی امکان بررسی این پرسش را فراهم می‌کند که آیا اثر مشاهده‌شده:

- در یک Model محدود است؛
- در چند Model تکرار می‌شود؛
- یا به تعامل Framework و قابلیت‌های Model وابسته است.

---

15. طراحی چندمصنوعی

یک Artifact واحد برای بررسی یک نمونه کافی است، اما برای استنتاج درباره یک الگوی عمومی کافی نیست.

در طراحی چندArtifact باید تا حد امکان تنوع معناداری در موارد زیر ایجاد شود:

- حوزه؛
- نوع متن؛
- پیچیدگی؛
- ساختار استدلال؛
- طول؛
- ویژگی‌های تحلیلی.

روش انتخاب و نمونه‌گیری Artifactها باید متناسب با سؤال مطالعه تعیین و پیش از تحلیل نهایی تا حد امکان از نتیجه‌محوری مصون شود.

---

16. ارزیابی خروجی

16.1. ارزیابی بر اساس Task

Judge باید خروجی را نسبت به Task و Artifact ارزیابی کند.

وجود اصطلاحات Framework-specific یا پیچیدگی ظاهری خروجی به‌خودی‌خود امتیاز مثبت محسوب نمی‌شود.

همچنین خروجی طولانی‌تر صرفاً به دلیل طول بیشتر نباید جریمه شود.

ملاک ارزیابی باید کیفیت و ارزش واقعی تحلیل نسبت به مسئله باشد.

---

17. انتخاب معیارهای ارزیابی

معیارها باید برای Task و Artifact مناسب باشند.

Judge می‌تواند معیارهای مورد نیاز را تعیین کند، مشروط بر اینکه:

- معیارها به Task مرتبط باشند؛
- تعریف عملیاتی روشنی داشته باشند؛
- بر اساس ویژگی‌های Framework به‌صورت پسینی تحمیل نشده باشند.

اگر معیارهای ثابتی برای یک مطالعه تعیین شده باشند، باید پیش از اجرای قضاوت ثبت شوند.

---

18. وزن‌دهی

اگر معیارها وزن داشته باشند، وزن‌ها باید پیش از محاسبه نتیجه نهایی مشخص باشند.

برای هر وزن باید امکان ثبت منطق انتخاب وجود داشته باشد.

وزن‌دهی نباید صرفاً به این دلیل تغییر کند که یک Condition نتیجه مطلوب مورد انتظار را نشان نداده است.

در صورت انجام تحلیل حساسیت (Sensitivity Analysis)، تغییر نتایج تحت وزن‌های جایگزین باید جداگانه گزارش شود.

---

19. استقلال قضاوت

استقلال Judge به معنای ناآگاهی او از وجود Framework نیست.

Judge می‌تواند بداند یک خروجی با یک Framework خاص تولید شده باشد، اما نباید نتیجه مورد انتظار یا معیارهای اختصاصی Framework به او تحمیل شود.

در صورت امکان، طراحی می‌تواند از شناسه‌های خنثی برای خروجی‌ها استفاده کند؛ اما Blind Evaluation الزام ذاتی Benchmark نیست.

معیار اصلی، استقلال فرآیند قضاوت است، نه الزام به Blind بودن.

---

20. منبع ارزیابی

Judge Resource برای کاهش ناآشنایی معنایی (Semantic Unfamiliarity) در ارزیابی Frameworkهای دارای اصطلاحات تخصصی مجاز است.

این منبع باید صرفاً اطلاعات لازم برای فهم معنای اصطلاحات و ساختارهای Framework را فراهم کند.

Judge Resource نباید:

- معیار ارزیابی تعیین کند؛
- وزن معیارها را تحمیل کند؛
- خروجی مطلوب را معرفی کند؛
- Framework را برتر معرفی کند؛
- اصطلاح Framework را معادل کیفیت فرض کند.

این اصل با طراحی Glossary Benchmark هم‌راستا است؛ Glossary موجود صراحتاً نقش خود را مرجع رفع ابهام معنایی می‌داند و آن را با معیار ارزیابی یکی نمی‌گیرد.

در صورت استفاده از Judge Resource، نسخه دقیق منبع باید همراه Run یا Evaluation Record ذخیره شود.

---

21. Self-Compare

Self-Compare باید به‌عنوان یک طراحی ارزیابی مستقل ثبت شود.

در این طراحی، Model یا سیستم تولیدکننده تحلیل، ارزیابی خروجی‌ها را نیز انجام می‌دهد.

Self-Compare برای مطالعه ارزیابی درون‌سیستمی معتبر است، اما نتیجه آن نباید بدون برچسب به‌عنوان معادل ارزیابی مستقل تلقی شود.

در صورت وجود منابع کافی، Self-Compare می‌تواند با Independent Judge مقایسه شود.

هدف این مقایسه، بررسی تفاوت الگوهای قضاوت است، نه الزاماً تعیین اینکه یکی از دو نوع Judge «صحیح» است.

---

22. ارزیابی مستقل

در طراحی‌های دارای Judge مستقل، Judge نباید به نتیجه Condition دیگر دسترسی داشته باشد، مگر آنکه طراحی صراحتاً مقایسه دوتایی (Pairwise Comparison) را ایجاب کند.

در Pairwise Comparison، اطلاعات لازم برای مقایسه باید در هر دو خروجی یکسان باشد.

اگر Judge به Framework identity یا Judge Resource دسترسی دارد، این موضوع باید ثبت شود.

---

23. ثبت مبنای قضاوت

در صورت استفاده از LLM Judge، علاوه بر امتیاز نهایی، در صورت امکان باید مبنای ساختاریافته قضاوت یا ردپای شواهد (Evidence Trace) ثبت شود.

ثبت این اطلاعات باید به‌گونه‌ای باشد که:

- مبنای قضاوت قابل بررسی باشد؛
- معیارهای به‌کاررفته مشخص باشند؛
- نتیجه صرفاً یک عدد غیرقابل تفسیر نباشد.

در عین حال، rationale تولیدشده توسط Judge نباید به‌عنوان حقیقت مستقل درباره Artifact تلقی شود.

---

24. اندازه‌گیری تغییر کیفیت

تغییر کیفیت میان دو Condition می‌تواند به‌صورت اختلاف نمره یا سایر معیارهای از پیش تعیین‌شده محاسبه شود:

[
\Delta Q = Q_F-Q_R
]

که در آن:

- Q_F: کیفیت Condition مربوط به Framework؛
- Q_R: کیفیت Condition مرجع.

تعریف عملیاتی Q باید در پروتکل مطالعه مشخص باشد.

\Delta Q یک اندازه مشاهده‌شده است و به‌خودی‌خود علت تغییر را اثبات نمی‌کند.

---

25. ارزش تحلیلی افزوده

ارزش تحلیلی افزوده باید جدا از کیفیت مطلق اندازه‌گیری شود.

ارزیابی باید مشخص کند که آیا Condition مداخله:

- تحلیل جدیدی ایجاد کرده؛
- ساختار تحلیلی موجود را غنی‌تر کرده؛
- محدودیت یا شکاف مشخصی را برطرف کرده؛
- یا صرفاً همان اطلاعات را با حجم بیشتر بازتولید کرده است.

این ارزیابی باید نسبت به Task و Artifact انجام شود.

بنابراین افزایش حجم خروجی معادل افزایش ارزش تحلیلی نیست.

---

26. پروتکل Drift

برای اندازه‌گیری Drift، یک Artifact و Task ثابت در چند Run مستقل اجرا می‌شود.

مقایسه می‌تواند در سطوح مختلف انجام شود:

سطح محتوایی

تغییر در ادعاها، استنتاج‌ها، روابط و عناصر تحلیلی.

سطح ساختاری

تغییر در سازمان‌دهی و روابط میان اجزای تحلیل.

سطح قضاوت

تغییر در ارزیابی Model درباره خروجی‌ها یا درباره کیفیت دو Condition.

نوع Drift مورد اندازه‌گیری باید پیش از تحلیل مشخص شود.

---

27. تکرار با مصنوع ثابت

یکی از طراحی‌های مهم Benchmark، اجرای تکراری تحلیل یک Artifact ثابت است.

در این طراحی:

[
A=constant
]

و در Runهای متعدد:

[
Analysis(A,M,C)_1,\ldots,Analysis(A,M,C)_n
]

با یکدیگر مقایسه می‌شوند.

این طراحی امکان مطالعه میزان تغییرپذیری Model در مواجهه با یک محتوای ثابت را فراهم می‌کند.

---

28. Judgment Drift

برای مطالعه Judgment Drift، یک مجموعه ثابت از خروجی‌ها می‌تواند در چند نوبت مورد قضاوت قرار گیرد.

در این حالت باید مشخص شود که:

- خروجی‌ها ثابت بوده‌اند یا نه؛
- Judge Model ثابت بوده یا نه؛
- Prompt قضاوت ثابت بوده یا نه؛
- Judge Resource ثابت بوده یا نه.

هر تغییر مشاهده‌شده باید در ارتباط با این شرایط تفسیر شود.

---

29. Framework-conditioned Drift

Drift می‌تواند برای RAW و Framework Condition به‌صورت جداگانه محاسبه شود.

برای نمونه:

[
D_{RAW}
]

و:

[
D_{Framework}
]

سپس می‌توان تفاوت میان آن‌ها را بررسی کرد.

این تفاوت به‌تنهایی ثابت نمی‌کند که Framework علت Drift است.

---

30. اندازه‌گیری Fidelity

تحقق Framework باید بر اساس specification همان Framework ارزیابی شود.

در صورتی که Framework دارای مراحل یا constraints مشخص باشد، باید بررسی شود که اجرای واقعی:

- کدام اجزا را اجرا کرده؛
- کدام اجزا را حذف کرده؛
- کدام اجزا را تغییر داده؛
- و کدام اجزا را به‌صورت مبهم اجرا کرده است.

این اطلاعات برای تفسیر نتایج Framework Condition ضروری است.

اجرای ناقص نباید بدون ثبت، به‌عنوان اجرای کامل Framework محسوب شود.

---

31. Attribution

Benchmark باید میان سه گزاره تمایز بگذارد:

1. تغییر مشاهده شد.
2. تغییر در حضور Framework مشاهده شد.
3. Framework علت تغییر بود.

گزاره سوم به شواهد قوی‌تری نیاز دارد.

برای افزایش قدرت Attribution باید تغییر در چند Model، Artifact و Run بررسی شود و متغیرهای جایگزین تا حد امکان کنترل شوند.

---

32. بار اجرایی

هزینه اجرای Framework باید جدا از کیفیت خروجی اندازه‌گیری شود.

موارد قابل ثبت:

- تعداد درخواست‌ها؛
- تعداد مراحل؛
- توکن‌های ورودی؛
- توکن‌های خروجی؛
- زمان اجرا؛
- تعداد Retry؛
- خطاهای Provider؛
- هزینه مالی، در صورت دسترس؛
- مصرف منابع خارجی.

این داده‌ها باید در کنار نتایج کیفیت گزارش شوند، نه اینکه به‌صورت خودکار در امتیاز کیفیت وارد شوند.

---

33. محدودیت زمینه و کوتاه‌سازی

اگر Artifact از ظرفیت Context Model بیشتر باشد، روش کاهش حجم باید پیش از اجرا مشخص شود.

روش‌های ممکن شامل:

- کوتاه‌سازی (Truncation)؛
- قطعه‌بندی (Segmentation)؛
- خلاصه‌سازی (Summarization)؛
- بازیابی (Retrieval)؛
- انتخاب (Selection).

روش انتخاب‌شده باید ثبت شود.

اگر بخش‌های متفاوت Artifact در دو Condition در دسترس Model قرار گیرند، مقایسه باید با ذکر این محدودیت گزارش شود.

---

34. بازیابی خارجی

اگر Framework یا Model از Retrieval یا منابع خارجی استفاده کند، سابقه و منشأ آن منابع باید ثبت شود.

حداقل اطلاعات:

- منبع؛
- شناسه؛
- زمان بازیابی؛
- Query، در صورت امکان؛
- محتوای بازیابی‌شده یا Hash/Identifier آن؛
- تفاوت منابع میان Conditions.

در صورت تفاوت منابع، اثر مشاهده‌شده نباید بدون بررسی به Framework نسبت داده شود.

---

35. مدیریت شکست

اجرای ناموفق نباید به امتیاز ساختگی تبدیل شود.

هر Run باید وضعیت اجرایی داشته باشد.

حداقل وضعیت‌ها:

- "valid"
- "failed"
- "incomplete"
- "invalid"

دلیل شکست باید ثبت شود.

Run فاقد خروجی معتبر یا فاقد Judge معتبر نباید به‌عنوان یک نتیجه معتبر وارد محاسبه شود.

این اصل با سازوکار Evidence Gate موجود در پیاده‌سازی Benchmark هم‌راستا است؛ اجرای ناقص نباید به‌عنوان benchmark evidence ذخیره شود و امتیاز ساختگی برای اجرای ناموفق ایجاد نمی‌شود.

---

36. داده مفقود

داده مفقود باید از داده صفر متمایز باشد.

برای مثال:

- نبودن امتیاز؛
- شکست Judge؛
- شکست Generation؛
- عدم دسترسی به متن کامل؛
- قطع‌شدن Provider

نباید به‌طور خودکار معادل "score = 0" در نظر گرفته شود.

قاعده برخورد با Missing Data باید پیش از Aggregation مشخص شود.

---

37. کنترل کیفیت

پیش از ورود یک Run به تحلیل نهایی باید کنترل‌های حداقلی انجام شود:

1. Artifact صحیح است؛
2. Model صحیح است؛
3. Condition صحیح است؛
4. Generation کامل است؛
5. خروجی قابل خواندن است؛
6. Judge معتبر است؛
7. Scoreها در دامنه مجاز هستند؛
8. Metadata ضروری موجود است.

Runهایی که این شروط را برآورده نمی‌کنند باید به‌عنوان "invalid" یا "incomplete" علامت‌گذاری شوند.

---

38. تجمیع

Aggregation باید مرحله‌ای باشد:

[
Run\rightarrow Artifact\rightarrow Model\rightarrow Domain\rightarrow Benchmark
]

نتیجه نهایی نباید تنها با یک میانگین کلی ارائه شود.

حداقل باید، در صورت امکان، موارد زیر نیز گزارش شوند:

- میانگین؛
- میانه؛
- پراکندگی؛
- دامنه؛
- تعداد نمونه‌ها؛
- تعداد Runهای نامعتبر؛
- تفاوت میان Modelها؛
- تفاوت میان Artifactها.

---

39. جلوگیری از تفسیر نوع Simpson

میانگین کلی ممکن است تفاوت‌های مهم میان گروه‌ها را پنهان کند.

برای مثال، اگر Framework در برخی Modelها اثر مثبت و در برخی اثر منفی داشته باشد، میانگین کلی ممکن است این ناهمگونی را پنهان کند.

بنابراین هر ادعای کلی باید در کنار توزیع اثرها و گروه‌بندی‌های اصلی بررسی شود.

---

40. برخورد آماری

روش آماری باید متناسب با ساختار داده انتخاب شود.

در طراحی‌های تکرارشونده، استقلال مشاهدات نباید بدون بررسی فرض شود.

در صورت وجود چند Run برای هر Model و Artifact، ساختار داده دارای وابستگی درون‌گروهی خواهد بود.

در مطالعات گسترده‌تر می‌توان از روش‌هایی استفاده کرد که Model و Artifact را به‌عنوان عوامل یا اثرهای ساختاری در نظر می‌گیرند.

روش آماری، فرض‌های آن و نحوه برخورد با Missing Data باید در گزارش مطالعه ثبت شود.

---

41. اندازه اثر و عدم قطعیت

گزارش تفاوت صرفاً بر اساس مقدار میانگین کافی نیست.

در صورت امکان باید همراه با موارد زیر ارائه شود:

- اندازه اثر (Effect Size)؛
- بازه عدم قطعیت (Interval Uncertainty)؛
- توزیع اثرها.

اگر روش آماری اجازه برآورد معتبر عدم قطعیت را ندهد، این محدودیت باید صریحاً گزارش شود.

---

42. مقایسه‌های متعدد

در مطالعاتی که تعداد زیادی Model، Artifact، معیار یا Condition مقایسه می‌شوند، خطر مثبت کاذب افزایش می‌یابد.

در چنین مطالعاتی باید:

- تعداد مقایسه‌ها ثبت شود؛
- روش کنترل مقایسه‌های متعدد، در صورت استفاده، مشخص شود؛
- نتایج اکتشافی از تأییدی تفکیک شوند.

---

43. اکتشافی در برابر تأییدی

Benchmark می‌تواند هم برای آزمون فرضیه‌های از پیش تعیین‌شده و هم برای کشف الگوها استفاده شود.

اما این دو وضعیت باید در گزارش از یکدیگر تفکیک شوند.

نتیجه‌ای که پس از مشاهده داده‌ها کشف شده است نباید بدون برچسب به‌عنوان فرضیه تأییدشده قبلی گزارش شود.

---

44. تحلیل ناهمگونی

اگر اثر Framework میان Modelها یا Artifactها متفاوت باشد، این تفاوت باید حفظ و گزارش شود.

برای مثال:

[
\Delta Q_{M_1}>0
]

در حالی که:

[
\Delta Q_{M_2}<0
]

می‌تواند نشان‌دهنده تعامل باشد.

این وضعیت به‌خودی‌خود شکست Benchmark نیست؛ ممکن است بخشی از یافته اصلی باشد.

---

45. تحلیل استحکام

نتیجه اصلی باید در برابر تغییرات معقول در روش ارزیابی بررسی شود.

در صورت امکان می‌توان موارد زیر را بررسی کرد:

- تغییر Judge؛
- تغییر وزن‌ها؛
- حذف یک Model؛
- حذف یک Artifact؛
- تغییر روش Aggregation؛
- تغییر روش برخورد با داده ناقص.

اگر نتیجه تنها تحت یک تنظیم خاص برقرار باشد، این وابستگی باید گزارش شود.

---

46. حساسیت به Judge

در مطالعات مهم، می‌توان یک خروجی را توسط بیش از یک Judge ارزیابی کرد.

هدف این کار تعیین میزان وابستگی نتیجه به Judge خاص است.

اگر اختلاف قابل توجهی مشاهده شود، نتیجه باید به‌صورت حساس به Judge گزارش شود، نه اینکه یکی از Judges بدون مبنای مستقل حذف شود.

---

47. Provenance

هر مقدار مشتق‌شده باید بتواند به داده‌های اولیه خود بازگردد.

حداقل زنجیره:

[
Derived\ Metric\rightarrow Evaluation\rightarrow Run\rightarrow Condition\rightarrow Artifact
]

این زنجیره باید از طریق شناسه‌های یکتا در داده‌های Benchmark قابل پیگیری باشد.

---

48. Benchmark Record

برای هر اجرای معتبر باید یک Record ساختاریافته ذخیره شود.

Record باید، در حد امکان، شامل موارد زیر باشد:

- شناسه Run؛
- Artifact ID؛
- Model ID؛
- Framework ID؛
- Condition؛
- Prompt Version؛
- Configuration؛
- Generation Output؛
- Execution Metadata؛
- Judge Metadata؛
- Evaluation Data؛
- Validation Status.

ساختار دقیق Schema باید در اسناد فنی Benchmark تعریف شود، نه در این سند.

---

49. حفظ داده خام

داده خام نباید تنها به نتایج Aggregate تبدیل و سپس حذف شود.

در صورت امکان باید موارد زیر حفظ شوند:

- Artifact Reference؛
- Prompt؛
- Raw Model Output؛
- Judge Input؛
- Judge Output؛
- Execution Metadata؛
- Error Logs؛
- Configuration.

اگر محدودیت حقوقی، حریم خصوصی یا فنی مانع ذخیره یک جزء شود، این محدودیت باید ثبت شود.

---

50. سطوح قابلیت بازتولید

قابلیت بازتولید را می‌توان در چند سطح گزارش کرد:

سطح ۱ — قابلیت بازتولید پیکربندی

تنظیمات و نسخه‌ها ثبت شده‌اند.

سطح ۲ — قابلیت بازتولید اجرا

Artifact، Prompt، Model و Configuration قابل بازسازی‌اند.

سطح ۳ — قابلیت بازتولید نتیجه

داده‌های خام و فرآیند محاسبه نتیجه قابل بازسازی‌اند.

سطح ۴ — تکرار مستقل

یک اجرای مستقل خارج از اجرای اولیه توانسته است پروتکل را بازتولید کند.

سطح ادعای Reproducibility باید با سطح واقعی شواهد منطبق باشد.

---

51. کنترل نسخه

هر Benchmark Run باید به نسخه‌های مورد استفاده قابل اتصال باشد.

حداقل موارد زیر باید قابل شناسایی باشند:

- Benchmark Specification Version؛
- Framework Version؛
- Prompt Version؛
- Judge Resource Version؛
- Model Identifier؛
- Evaluation Configuration.

تغییر هر جزء مهم می‌تواند یک اجرای جدید یا نسخه جدید Benchmark Record ایجاد کند.

---

52. طبقه‌بندی شواهد

هر نتیجه باید از نظر وضعیت شواهد طبقه‌بندی شود.

تمایز حداقلی:

- مشاهده مستقیم؛
- اندازه‌گیری مشتق‌شده؛
- تفسیر؛
- استنتاج تعمیم‌یافته.

نتیجه‌ای که فقط در یک Run مشاهده شده نباید با نتیجه‌ای که در چند Model و Artifact تکرار شده هم‌سطح گزارش شود.

---

53. حدود ادعا

Benchmark می‌تواند درباره رفتار مشاهده‌شده در شرایط آزمایش ادعا کند.

اما نتایج نباید بدون شواهد کافی به گزاره‌هایی مانند موارد زیر تبدیل شوند:

- Framework ذاتاً برتر است؛
- Framework علت قطعی بهبود است؛
- Model ذاتاً توانمندتر است؛
- Stability به معنای Correctness است؛
- افزایش امتیاز معادل Truth است؛
- نتیجه یک Artifact به همه حوزه‌ها تعمیم‌پذیر است.

---

54. پروتکل گزارش‌دهی

گزارش نهایی باید دست‌کم شامل این اجزا باشد:

1. Research Question؛
2. Experimental Design؛
3. Artifact Selection؛
4. Model Configuration؛
5. Framework Version؛
6. Conditions؛
7. Prompt/Execution Carrier؛
8. Judge Configuration؛
9. Judge Resource؛
10. Number of Runs؛
11. Exclusion and Failure Rules؛
12. Quality Results؛
13. Incremental Value Results؛
14. Drift Results؛
15. Fidelity Results؛
16. Execution Burden؛
17. Uncertainty؛
18. Robustness/Sensitivity Analysis؛
19. Limitations؛
20. Reproducibility Information.

---

55. حداقل مقایسه معتبر

یک مقایسه حداقلی زمانی معتبر تلقی می‌شود که:

- Artifact مشخص باشد؛
- Task مشخص باشد؛
- Model مشخص باشد؛
- RAW و Framework Conditions قابل تشخیص باشند؛
- خروجی هر دو Condition موجود باشد؛
- Judge معتبر باشد؛
- معیار ارزیابی قابل شناسایی باشد؛
- Metadata ضروری ثبت شده باشد.

در صورت فقدان هر یک از این عناصر، نتیجه باید با وضعیت مناسب علامت‌گذاری شود.

---

56. حداقل شواهد برای ادعا در سطح Framework

ادعای مربوط به یک Framework در سطح کلی نباید از یک مقایسه منفرد استخراج شود.

برای افزایش قدرت چنین ادعایی، شواهد باید در چند سطح تکرار شوند:

[
Run\rightarrow Artifact\rightarrow Model
]

و در صورت امکان:

[
\rightarrow Domain
]

هرچه نتیجه در شرایط مستقل بیشتری تکرار شود، مبنای استنتاج درباره یک الگوی نظام‌مند قوی‌تر خواهد بود.

---

57. تفسیر نتایج منفی

عدم مشاهده بهبود نیز یک نتیجه معتبر است.

اگر Framework در شرایط مشخص:

- کیفیت را تغییر ندهد؛
- ارزش افزوده محدودی ایجاد کند؛
- Drift را افزایش دهد؛
- یا هزینه اجرا را افزایش دهد بدون آنکه ارزش متناسبی ایجاد کند،

این یافته نباید به‌عنوان شکست روش‌شناسی تلقی شود.

Benchmark باید امکان ثبت اثرهای مثبت، خنثی و منفی را به یک اندازه فراهم کند.

---

58. تفسیر نتایج ترکیبی

اگر نتایج میان Modelها یا Artifactها متفاوت باشند، نتیجه باید به‌صورت شرطی گزارش شود.

برای مثال:

«Framework در مجموعه آزمایش‌شده در برخی شرایط بهبود و در برخی شرایط کاهش کیفیت نشان داد.»

نباید چنین الگویی با یک میانگین کلی به یک ادعای مطلق تبدیل شود.

---

59. تحلیل هزینه ـ ارزش

در مطالعاتی که هزینه اجرا اهمیت دارد، Quality و Incremental Value باید در کنار Execution Burden تحلیل شوند.

یک Framework ممکن است:

- ارزش تحلیلی بالایی ایجاد کند و هزینه بالایی داشته باشد؛
- ارزش اندکی ایجاد کند و هزینه بالایی داشته باشد؛
- ارزش متوسط با هزینه پایین ایجاد کند؛
- یا اثر قابل توجهی ایجاد نکند.

Benchmark نباید بدون تعریف قبلی یک Trade-off واحد را به همه کاربردها تحمیل کند.

---

60. پیاده‌سازی مستقل از Framework

Implementation Benchmark باید امکان ارزیابی Frameworkهای مختلف را بدون تغییر در منطق اصلی مقایسه فراهم کند.

هیچ Framework نباید برای ورود به Benchmark مجبور شود ساختار مفهومی خود را با BSI تطبیق دهد.

آنچه باید استاندارد شود:

- ثبت شرایط؛
- تولید Record؛
- Validation؛
- Evaluation Interface؛
- Provenance؛
- Reporting.

نه معماری داخلی Framework.

---

61. رابطه با BSI

BSI در این Benchmark یک Framework قابل ارزیابی است.

ویژگی‌های اختصاصی BSI، از جمله اصطلاحات، ابعاد داخلی، روش‌های تحلیلی و شاخص‌های اختصاصی، نباید به‌صورت پیش‌فرض به معیار عمومی Benchmark تبدیل شوند.

در نتیجه:

[
BSI\subset Frameworks_{Evaluated}
]

و:

[
BSI\ Benchmark\neq BSI\ Evaluation\ Rubric
]

Benchmark زیرساخت مقایسه است؛ Framework یکی از موضوعات مقایسه.

---

62. رابطه با Ontology

این سند عمداً از بازتعریف مفاهیم اصلی خودداری می‌کند.

تقسیم مسئولیت چنین است:

موضوع| Ontology| Methodology
تعریف مفاهیم| ✓| —
تعریف روابط| ✓| —
تعریف Condition| ✓| —
تعریف Judge| ✓| —
تعریف Drift| ✓| —
نحوه ایجاد Condition| —| ✓
نحوه Sampling| —| ✓
نحوه Repeated Run| —| ✓
نحوه قضاوت| —| ✓
نحوه محاسبه| —| ✓
نحوه Aggregation| —| ✓
کنترل خطا| —| ✓
گزارش Reproducibility| —| ✓

هرجا Implementation با تعریف Ontology ناسازگار باشد، Implementation باید اصلاح شود یا محدودیت آن صریحاً مستند شود؛ معنای Construct نباید صرفاً برای تطبیق با Implementation تغییر کند.

---

63. اصل یکپارچگی داده

هیچ داده‌ای نباید صرفاً برای کامل‌کردن جدول نتایج ساخته شود.

به‌ویژه:

- "failure ≠ zero"
- "missing ≠ zero"
- "unavailable ≠ poor quality"
- "invalid ≠ low score"

داده نامعتبر باید نامعتبر باقی بماند.

این اصل برای جلوگیری از ایجاد شواهد مصنوعی و حفظ قابلیت ممیزی Benchmark ضروری است.

---

64. قابلیت ممیزی

هر نتیجه مهم باید قابلیت ممیزی داشته باشد.

ممیز باید بتواند تا حد امکان مسیر زیر را دنبال کند:

[
Claim\rightarrow Result\rightarrow Metric\rightarrow Evaluation\rightarrow Output\rightarrow Run\rightarrow Configuration
]

اگر یک بخش از این زنجیره قابل بازیابی نیست، محدودیت آن باید اعلام شود.

---

65. اصول اجرایی نهایی

روش اجرای Benchmark باید بر اصول زیر منطبق باشد:

1. هر مقایسه بر یک Task و Artifact مشخص بنا شود.
2. RAW و Framework Conditions تا حد امکان هم‌تراز باشند.
3. Model، Prompt و Configuration قابل شناسایی باشند.
4. اجرای مستقل از State قبلی انجام شود.
5. Repeated Runs برای مطالعه نوسان و Drift استفاده شوند.
6. Judge معیارهای مناسب Task را مستقل از ویژگی‌های Framework تعیین کند.
7. Judge Resource فقط نقش رفع ابهام معنایی داشته باشد.
8. Self-Compare به‌عنوان یک Design مستقل گزارش شود.
9. Framework Fidelity جدا از Quality سنجیده شود.
10. Drift با Stability یا Correctness یکی گرفته نشود.
11. Execution Burden جداگانه ثبت شود.
12. Failure و Missing Data به Score مصنوعی تبدیل نشوند.
13. داده خام تا حد امکان حفظ شود.
14. هر Metric به داده اولیه خود قابل ردیابی باشد.
15. Aggregation نباید ناهمگونی Model و Artifact را پنهان کند.
16. نتایج Exploratory و Confirmatory از یکدیگر تفکیک شوند.
17. عدم بهبود و نتایج منفی نیز به‌عنوان یافته معتبر حفظ شوند.
18. ادعای علّی متناسب با قدرت طراحی تجربی باشد.
19. ادعاهای Framework-level به شواهد تکرارشده نیاز داشته باشند.
20. هیچ Frameworkی در طراحی Benchmark برتر فرض نشود.

---

66. تعریف عملیاتی فشرده

BSI Benchmark یک پروتکل مقایسه‌ای و تکرارشونده است که در آن یک Task تحلیلی روی Artifactهای مشخص، تحت Conditions قابل ثبت، با و بدون یک Framework اجرا می‌شود؛ خروجی‌ها با استفاده از یک فرآیند ارزیابی مستقل و قابل ممیزی بررسی می‌شوند؛ و تفاوت‌های مشاهده‌شده در کیفیت، ارزش تحلیلی، تحقق Framework، Drift، Stability و بار اجرایی در سطح Run، Artifact و Model ثبت و تحلیل می‌شوند.

قدرت هر نتیجه تابع تعداد و تنوع اجراها، کیفیت کنترل Conditions، استقلال ارزیابی، قابلیت ردیابی داده و میزان تکرارپذیری آن است.

---

67. اصل نهایی روش‌شناسی

روش‌شناسی Benchmark باید پیش از هر چیز قابل بازبینی، قابل تکرار و مقاوم در برابر نتیجه‌گرایی باشد.

هدف آن تولید یک برنده از پیش تعیین‌شده نیست؛ هدف ایجاد شرایطی است که در آن اثر یک Framework بتواند در تعامل واقعی با Model و Artifact مشاهده، اندازه‌گیری، مقایسه و در سطح متناسب با شواهد تفسیر شود.

در این چارچوب، یک نتیجه معتبر لزوماً نتیجه‌ای نیست که بهبود را نشان دهد؛ نتیجه معتبر نتیجه‌ای است که از یک طراحی شفاف، داده قابل ردیابی، ارزیابی مستقل و قواعد از پیش مشخص‌شده به‌طور مشروع به دست آمده باشد.
